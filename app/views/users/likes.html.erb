<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<div class="col-sm-8">
    <ul class="nav nav-tabs nav-justified mb-3">
      
      <li class="nav-item"><a href="
<%= user_path(@user.id) %>
" class="nav-link
<%= 'active' if current_page?(user_path(@user.id)) %>
">自分の投稿
<span class="badge badge-secondary">
<%= @count_posts %>
</span></a></li>
      
      <li class="nav-item"><a href="
<%= likes_user_path(@user.id) %>
" class="nav-link
<%= 'active' if current_page?(likes_user_path(@user.id)) %>
">お気に入り
<span class="badge badge-secondary">
<%= @count_favorites %>
</span></a></li>

    </ul>
    <%= render 'posts/posts', posts: @fav_posts %>
</div>



<%= link_to 'Back', root_path %>
